import ContactForm from '@/components/ContactForm';

export const metadata = {
  title: 'Contact Us | MediCare Connect',
  description: 'Get in touch with the MediCare Connect support team.',
};

export default function ContactPage() {
  return <ContactForm />;
}
